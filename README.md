# Tredence Case Study

This repository contains the full-stack implementation for the Tredence case study.

## Installation

To get started, clone the repository and install the necessary dependencies:

```bash
npm install
```

## Usage

Run the application in development mode:

```bash
npm run dev
```

This will start the server and you can access it in your browser.